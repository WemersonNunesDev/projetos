import styles from '../index.module.css';

function Header() {
    return (
        <header className={styles.bar_menu}>
            <nav>
                <a href="#">Home</a>
                <a href="#">Home</a>
                <a href="#">Home</a>
            </nav>
        </header>
    )
}

export default Header;